# Lab 7

1. Within a Github action that runs whenever code is pushed because tests do their job best when being contantly run and used to evaluate code incrementally, not all at once. 
2. No because you would use a unit test to test the output of just one single function. E2E tests usually aim to test the interaction of different functions that occur when a user is using a software.
3. The difference between navigation and snapshot mode is that navigation mode will load the page up and base its performance around that. On the other hand, snapshot mode takes whatever state the page is in when the test is run and analyzes it solely on that. Both of these modes are good at evaluating different things: navigation is good at analyzing general performance and efficiency of the website while snapshot is good at determining accessibility. 
4. Add a lang attribute to the html element, include metadata description in documentation, requests are chained together which makes the download size of resources larger and impacts performance on mobile. 







